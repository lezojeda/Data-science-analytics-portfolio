---
title: "Stomatal conductance analysis"
author: "Lucas Ezequiel Ojeda"
date: "September 9, 2018"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r, echo = FALSE}
setwd("C:\\Users\\lucas\\Dropbox\\Carrera\\Sorgo\\Datos")
library("readxl")
library("dplyr")
se <- function(x) sd(x)/sqrt(length(x)) #Función para calcular error estándar
```

Some descriptive analysis:

```{r, echo = FALSE}
df <- read_excel("Datos_PH.xlsx")
df$Sal <- as.factor(df$Sal)
df$Tiempo <- as.factor(df$Tiempo)
```

```{r}
str(df)
```


